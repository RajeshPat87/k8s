apiVersion: v1
kind: Pod
metadata:
  name: too-big-pod
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    resources:
      limits:
        memory: "2Gi" # This is MORE than your LimitRange allows!