apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:                  # <--- Add this section
    app: busybox-app       # <--- This is the key/value pair
spec:
  containers:
  - name: busybox
    image: busybox
    command: ['sh', '-c', 'echo Hello Kubernetes && sleep 3600']