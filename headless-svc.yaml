apiVersion: v1
kind: Service
metadata:
  name: stateful-svc
  labels:
    app: stateful-demo
spec:
  ports:
  - port: 80
    name: web
  clusterIP: None # This makes it "headless"
  selector:
    app: stateful-demo