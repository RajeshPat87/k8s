apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: web    # This must match the label on your Pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80