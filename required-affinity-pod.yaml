apiVersion: v1
kind: Pod
metadata:
  name: affinity-demo
spec:
  containers:
  - name: nginx-ssd
    image: nginx:latest
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: disktype
            operator: In
            values:
            - ssd